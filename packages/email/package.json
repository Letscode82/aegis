{
  "name": "@aegis/email",
  "version": "0.1.0",
  "private": true,
  "description": "Shared transactional email delivery — provider-abstracted, zero-dependency HTTP mailers, degrade-to-logged when unconfigured",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint src tests",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@aegis/eslint-config": "workspace:*",
    "@types/node": "^20.17.9",
    "eslint": "^8.57.1",
    "typescript": "5.6.3",
    "vitest": "1.6.1"
  }
}
