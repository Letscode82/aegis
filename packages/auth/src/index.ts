/**
 * @aegis/auth — Auth0 integration + permission model.
 *
 * Empty in Step 1. Step 3 (PR #3) adds:
 *   - @auth0/nextjs-auth0 wiring (login/logout/session)
 *   - useCurrentUser() hook
 *   - the canonical Permission enum
 *   - role-to-permissions mapping (admin, gc, attorney, paralegal,
 *     legal_ops, requester, external_counsel, viewer)
 *   - canUserDo(user, action, resource) helper
 */
export {};
